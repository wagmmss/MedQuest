# Normaliza fim de linha: LF no repositório, nativo no working copy.
* text=auto

# Sempre LF (scripts que podem rodar em Linux/container no deploy)
*.py    text eol=lf
*.sh    text eol=lf
*.sql   text eol=lf

# Binários: nunca tocar
*.png   binary
*.jpg   binary
*.jpeg  binary
*.ico   binary
*.pdf   binary
*.db    binary
*.zip   binary
