import { FlashcardClient } from "./FlashcardClient";

export default function RevisaoAtivaPage() {
  return (
    <div className="animate-in fade-in duration-500 w-full h-full">
      <FlashcardClient />
    </div>
  );
}
